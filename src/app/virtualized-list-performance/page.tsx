import React from "react";

import VirtualizedList from "./VirtualizedList";

const page = () => {
    return (
        <div>
            <VirtualizedList />
        </div>
    );
};

export default page;
