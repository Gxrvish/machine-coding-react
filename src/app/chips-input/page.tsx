import ChipsInput from "./ChipsInput";

const Page: React.FC = ({}) => {
    return (
        <div className="h-screen flex justify-center items-center">
            <ChipsInput />
        </div>
    );
};

export default Page;
